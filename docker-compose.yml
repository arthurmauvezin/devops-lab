version: '3'
services:
  my-service: 
    image: arnaudhdla/image_webapp
    ports:
      - 3000
      - 8080
    environment:
      MYSQL_HOST : duckerHost
      MYSQL_PORT : duckerPort
      MYSQL_DATABASE : duckerDataBase
      MYSQL_USER : duckerUser
      MYSQL_PASSWORD : duckerPassword