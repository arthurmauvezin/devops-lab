version: '3'
services:
  my-service:
    ports:
      - "3000:80"
    environment:
      - MYSQL_HOST=localhost
      - MYSQL_LOGIN=root
      - MYSQL_PASSWORD=
      - MYSQL_DATABASE=zoo
      - MYSQL_PORT=3306
    image: godlikedocker/devops-lab