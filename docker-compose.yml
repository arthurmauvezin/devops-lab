version: '3'
services:
  webService:
    ports:
      - "3000:3000"
    environment:
      - MYSQL_HOST=host.docker.internal
      - MYSQL_DATABSE=project
      - MYSQL_PORT=8889
      - MYSQL_USER=root
      - MYSQL_PASSWORD=root
    image: antoinebattagliotti/devops-lab:webService