version: '3'
services:
  my-service:
    image: natna25/devops-lab

ports:
  -"3000:3000"
  expose:
    - 3000
environment:
  MYSQL_HOST: localhost
  MYSQL_USER: root
  MYSQL_PASSWORD: 
  MYSQL_DATABASE: nodejs
  MYSQL_PORT: 3306