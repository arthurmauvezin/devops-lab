/*
const mysql = require('mysql')

const pool = mysql.createPool({
  connectionLimit: 10,
  host: 'localhost',
  user: 'root', // To change with your MySQL username
  password: 'root', // To change with your MySQL password
  database: 'webservice',
  port: 3306
})

exports.getConnection = function getConnection() {
  return pool
}
*/
